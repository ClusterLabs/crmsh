---
- name: Cluster health check
  description: >
    This script executes an extensive health check utility and warns
    about any potential issues with the cluster.

  steps:
    - name: Gather cluster state
      hahealth: mode=gather
      where: all

    - name: Check cluster health
      hahealth: mode=verify
      where: local
