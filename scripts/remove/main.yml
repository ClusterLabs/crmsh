---
- name: Removes a node from a cluster
  description: >
    Removes the node both from Pacemaker and Corosync.
  parameters:
    - name: node
      description: Node to remove from the cluster
  steps:
    - name: Check state of nodes
      type: collect
      call: collect.py

